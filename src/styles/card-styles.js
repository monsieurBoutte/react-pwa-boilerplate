import { makeStyles } from '@material-ui/styles';

export const useCardStyles = makeStyles({
  baseCard: {
    borderRadius: '.5rem',
    marginRight: 'auto',
    marginLeft: 'auto',
    maxWidth: 450
  },
  title: {
    textAlign: 'center'
  }
});
